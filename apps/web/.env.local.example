# Backend API base URL. Copy to .env.local.
# Used by both server components (SSR) and the browser, so use a host both can
# reach (localhost works when the web app and backend run on the same machine).
NEXT_PUBLIC_API_URL=https://nirmaan-backend-production.up.railway.app/api/v1
